/*
============================================================
  Gulp Default Task
============================================================
*/

// load config
var config = require('../config/index');

// load dependencies
var gulp = require('gulp');

gulp.task('default', function() {
  return;
});
